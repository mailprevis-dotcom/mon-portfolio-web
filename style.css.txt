/* --- 1. VARIABLES GLOBALES (Thème Sombre et Énergique) --- */
:root {
    --couleur-principale: #e74c3c; /* Rouge/Orange Vif pour l'énergie */
    --couleur-fond-principal: #1f2a38; /* Bleu nuit très foncé */
    --couleur-fond-secondaire: #2c3e50; /* Gris bleu pour les sections */
    --couleur-texte: #ecf0f1; /* Blanc cassé */
    --couleur-texte-secondaire: #95a5a6; /* Gris clair */
    --espacement-grand: 60px;
    --espacement-moyen: 30px;
}

/* --- 2. RÉINITIALISATION ET FOND DE PAGE --- */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    scroll-behavior: smooth; /* Défilement doux entre les sections */
}

body {
    font-family: 'Arial', sans-serif;
    background-color: var(--couleur-fond-principal);
    color: var(--couleur-texte);
    line-height: 1.6;
}

/* --- 3. HEADER ET NAVIGATION --- */
.header {
    background-color: var(--couleur-fond-secondaire);
    padding: var(--espacement-moyen) 0;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
}

.header h1 {
    text-align: center;
    font-size: 2rem;
    color: var(--couleur-principale);
    text-transform: uppercase;
    letter-spacing: 2px;
}

.header nav ul {
    list-style: none;
    text-align: center;
    margin-top: 15px;
}

.header nav ul li {
    display: inline;
    margin: 0 15px;
}

.header nav ul li a {
    color: var(--couleur-texte);
    text-decoration: none;
    font-weight: 600;
    padding: 5px 10px;
    transition: background-color 0.3s;
    border-radius: 5px;
}

.header nav ul li a:hover {
    background-color: var(--couleur-principale);
    color: var(--couleur-fond-principal);
}

/* --- 4. STYLE GÉNÉRAL DES SECTIONS --- */
section {
    padding: var(--espacement-grand) 5%;
    min-height: 90vh; 
}

section h2 {
    font-size: 2.8rem;
    color: var(--couleur-principale);
    margin-bottom: 40px;
    padding-bottom: 10px;
    text-transform: uppercase;
    letter-spacing: 1px;
    border-bottom: 2px solid var(--couleur-principale);
    display: inline-block;
}

/* --- 5. SECTION ACCUEIL (HERO) --- */
.hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    
    /* Image de fond avec superposition (Overlay) pour la lisibilité */
    background-image: linear-gradient(rgba(31, 42, 56, 0.85), rgba(31, 42, 56, 0.85)), 
                      url('uploaded:1000031758.jpg-04f8d16d-5d4f-4a80-9d3d-297a7c452be8');
    background-size: cover;
    background-position: center;
    background-attachment: fixed; /* Rend l'image fixe lors du défilement */
    position: relative;
}

/* Contenu centré au-dessus de l'image de fond */
.hero-content {
    z-index: 2; /* S'assure que le contenu est au-dessus du fond */
    max-width: 900px;
}

.hero p {
    font-size: 1.3rem;
    max-width: 800px;
    color: var(--couleur-texte-secondaire);
    margin-bottom: var(--espacement-moyen);
}

.cta-button {
    display: inline-block;
    background-color: var(--couleur-principale);
    color: var(--couleur-texte);
    text-decoration: none;
    padding: 15px 30px;
    border-radius: 50px;
    font-weight: bold;
    font-size: 1.1rem;
    margin-top: 20px;
    transition: background-color 0.3s, transform 0.3s;
}

.cta-button:hover {
    background-color: #c0392b; /* Rouge plus foncé */
    transform: translateY(-3px);
}

/* Style de la Section Statistiques */
.stats-container {
    display: flex;
    justify-content: center;
    gap: 30px;
    margin: 40px 0;
}

.stat-item {
    background-color: rgba(44, 62, 80, 0.8); /* Fond semi-transparent */
    padding: 20px 30px;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    border: 1px solid var(--couleur-principale);
}

.stat-number {
    display: block;
    font-size: 2.5rem;
    font-weight: bold;
    color: var(--couleur-principale);
    line-height: 1;
    margin-bottom: 5px;
}

.stat-label {
    font-size: 0.9rem;
    color: var(--couleur-texte-secondaire);
    text-transform: uppercase;
}

/* --- 6. GALERIE PHOTOS ET VIDÉOTHÈQUE (GRILLES) --- */
.gallery, .videotheque {
    background-color: var(--couleur-fond-secondaire);
}

/* Styles pour la grille de la galerie (photos) */
.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* 3 colonnes sur desktop, s'adapte en mobile */
    gap: 20px;
    margin-top: 40px;
}

.gallery-item {
    background-color: var(--couleur-fond-principal);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
    transition: transform 0.3s;
}

.gallery-item:hover {
    transform: scale(1.03);
}

.gallery-item img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 16 / 10; /* Garde les images dans un ratio uniforme */
    object-fit: cover;
}

.caption {
    padding: 15px;
    font-size: 0.95rem;
    color: var(--couleur-texte-secondaire);
}

/* Styles pour la grille de la vidéothèque */
.video-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 30px;
    margin-top: 40px;
    justify-content: center;
}

.video-item {
    background-color: var(--couleur-fond-principal);
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
    overflow: hidden;
    padding-bottom: 15px;
}

.video-item iframe {
    width: 100%;
    height: 300px; /* Hauteur fixe pour l'iFrame */
}

.videotheque .note {
    margin-top: 40px;
    font-size: 0.9rem;
    color: var(--couleur-principale);
}

/* --- 7. SECTION CONTACT ET FOOTER --- */
.contact {
    text-align: center;
    background-color: var(--couleur-fond-principal);
}

.contact p {
    font-size: 1.2rem;
    margin: 15px 0;
}

/* Style des liens de contact */
.contact-link {
    color: var(--couleur-principale);
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s;
}

.contact-link:hover {
    color: var(--couleur-texte);
    text-decoration: underline;
}

/* Style des icônes de réseaux sociaux */
.social-links .social-icon {
    font-size: 2.5rem;
    margin: 0 15px;
    transition: transform 0.3s, opacity 0.3s;
    display: inline-block;
}

.social-links .social-icon:hover {
    transform: scale(1.1);
    opacity: 0.8;
}

.footer {
    background-color: #111;
    color: var(--couleur-texte-secondaire);
    text-align: center;
    padding: 20px 0;
    margin-top: 0;
}

/* --- 8. ADAPTATION MOBILE --- */
@media (max-width: 900px) {
    .gallery-grid {
        grid-template-columns: 1fr 1fr; /* 2 colonnes sur tablette */
    }
    .video-grid {
        grid-template-columns: 1fr; /* 1 colonne sur mobile */
    }
}

@media (max-width: 600px) {
    .gallery-grid {
        grid-template-columns: 1fr; /* 1 colonne sur téléphone */
    }
    .header h1 {
        font-size: 1.5rem;
    }
    .header nav ul li {
        display: block;
        margin: 5px 0;
    }
    /* Réduction de la taille des icônes sur mobile */
    .social-links .social-icon {
        font-size: 2rem;
        margin: 0 10px;
    }
    /* Adaptation de la section statistiques sur mobile */
    .stats-container {
        flex-direction: column;
        gap: 15px;
    }
    .stat-item {
        padding: 15px 20px;
    }
}